# Repository Agent Instructions

Read the nearest applicable `AGENTS.md` before modifying files.

Frontend work is governed by `src/AGENTS.md`.

Do not modify backend code while completing frontend-only tasks unless the user explicitly expands the task scope.

Keep changes focused on the requested feature. Run relevant lint, type-check, test, and build commands after implementation.
